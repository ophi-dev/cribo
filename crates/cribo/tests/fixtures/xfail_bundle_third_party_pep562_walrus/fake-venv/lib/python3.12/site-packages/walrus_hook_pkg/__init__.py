"""Package installing its PEP 562 hook through a named expression."""


def _resolve(name):
    if name == "lazy_value":
        return "resolved lazily"
    raise AttributeError(name)


(__getattr__ := _resolve)
