"""Package loading its backend through an aliased dynamic import."""

from importlib import import_module as load

backend_name = "json_backend"
backend = load(f".{backend_name}", __package__)


def backend_kind():
    return backend.KIND
