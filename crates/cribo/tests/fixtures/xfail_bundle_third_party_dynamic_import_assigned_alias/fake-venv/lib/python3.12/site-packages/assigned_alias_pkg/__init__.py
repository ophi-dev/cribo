"""Package whose dynamic-import callable is created by assignment."""

import importlib

load = importlib.import_module

backend_name = "json_backend"
backend = load(f".{backend_name}", __package__)


def backend_kind():
    return backend.KIND
