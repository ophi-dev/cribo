"""Package holding its import callable behind a class attribute."""

import importlib


class Loader:
    load = staticmethod(importlib.import_module)


backend = Loader.load("attr_alias_pkg.backend")


def backend_kind():
    return backend.KIND
