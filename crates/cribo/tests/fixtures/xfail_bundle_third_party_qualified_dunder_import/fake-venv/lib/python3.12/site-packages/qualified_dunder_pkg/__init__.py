"""Package loading its backend through a qualified builtins.__import__ call."""

import builtins

backend_name = "qualified_dunder_pkg.json_backend"
_module = builtins.__import__(backend_name, fromlist=["KIND"])


def backend_kind():
    return _module.KIND
