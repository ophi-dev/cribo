"""Package loading its backend through a functools.partial import wrapper."""

import functools
from importlib import import_module

load = functools.partial(import_module, package=__package__)

backend_name = ".json_backend"
backend = load(backend_name)
