"""Package installing its PEP 562 hook through tuple destructuring."""


def _resolve(name):
    if name == "lazy_value":
        return "resolved lazily"
    raise AttributeError(name)


__getattr__, MARKER = (_resolve, 1)
