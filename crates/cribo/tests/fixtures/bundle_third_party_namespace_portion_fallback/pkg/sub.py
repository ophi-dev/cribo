# A namespace-portion child with the SAME name as the site-packages child:
# Python selects the regular package first and loads ITS sub, never this one.
VALUE = "namespace portion child must not win"
