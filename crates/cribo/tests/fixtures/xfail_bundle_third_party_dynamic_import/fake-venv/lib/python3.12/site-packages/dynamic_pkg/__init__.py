"""Package loading its backend through a non-literal dynamic import."""

import importlib

backend_name = "json_backend"
backend = importlib.import_module(f".{backend_name}", __package__)


def backend_kind():
    return backend.KIND
