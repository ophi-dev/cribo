"""Package aliasing the importer through tuple destructuring."""

import importlib

load, = (importlib.import_module,)

backend = load("destructured_alias_pkg.backend")


def backend_kind():
    return backend.KIND
