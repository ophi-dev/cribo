"""Package registering a PEP 562 __getattr__ hook inside module-level control flow."""

try:
    from ._accel import __getattr__  # noqa: F401
except ImportError:

    def __getattr__(name):
        if name == "lazy_value":
            return "resolved lazily"
        raise AttributeError(name)
