"""Package loading its backend through a callable assigned from __import__."""

load = __import__

backend_name = "dunder_alias_pkg.json_backend"
_module = load(backend_name, fromlist=["KIND"])


def backend_kind():
    return _module.KIND
