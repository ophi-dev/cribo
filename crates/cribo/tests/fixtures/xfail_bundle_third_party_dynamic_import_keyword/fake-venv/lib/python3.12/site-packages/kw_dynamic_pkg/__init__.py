"""Package loading its backend through a non-literal keyword dynamic import."""

import importlib

backend_name = "json_backend"
backend = importlib.import_module(name=f".{backend_name}", package=__package__)


def backend_kind():
    return backend.KIND
